<span class="back-to-search">
  <a href="#"><== Back to Search Results</a>
</span>
<div id="occasionResultDetails">
    <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">
                {{catererName}}     lunch menu
                <button class="btn btn-xs btn-primary pull-right" id="btn-checkout" type = "button">
                  <i class="fa fa-check-circle"></i>  Checkout  </button>
            </h3>
          </div>
          <div class="panel-body">
            <div class="container">
              {{#each menuArray}}
              <div class="row menu-details">
                <div class="media">
                  <a href="#" class="pull-left">
                      <img src="images/items/{{this.image}}" style="height: 200px; width: 200px;" class="media-object" alt="Menu Image">
                  </a>
                  <div class="media-body">
                      <h4 class="media-heading">{{this.cuisine_name}} 
                        <small><i class="pull-right"> ${{this.item_price}} </i></small>
                      </h4>
                      <div class="menu-desc">
                        Excellent feature! I love it. One day I'm definitely going to put this Bootstrap component into use and I'll let you know once I do.
                      </div>
                      <form class="form-inline pull-right">
                        <div class="form-group">
                          <label for="quantity-{{@index}}" class="sr-only">Quantity: </label>
                          <input type="text" class="form-control item-quantity" placeholder="Quantity">
                        </div>
                        <button class ="btn btn-default btn-add-to-cart" 
                          data-catarer-name="{{../catererName}}"
                          data-catarer-id="{{this.caterer_id}}"
                          data-cuisine-name="{{this.cuisine_name}}"
                          data-cuisine-id="{{this.cuisine_id}}"
                          data-cuisine-image="{{this.image}}"
                          data-item-price="{{this.item_price}}">
                          <i class="fa fa-lg fa-cart-plus"></i>
                        </button>
                      </form>
                  </div>
                </div>
              </div>
              {{/each}}
            </div>
          </div>
        </div>
    </div>
</div>